<div class="content_container main_container">
    <h4 class="promo_main_header">
        <a href="/jobs">< back</a>
    </h3>
    <div class="row" id="promo_details_container">
        <script id="promo_details_template" type="x-tmpl-mustache/text">
            <div class="col-md-8">
                <h2 class="promo_list_name">{{name}}</h2>
                <p class="promo_dates underline">{{dates}}</p>
                <p>
                    <span class="promo_store">{{store_name}}</span>
                    <span class="promo_store">{{job_type}}</span>
                    <span class="promo_dates">{{published_on}}</span>
                </p>
                <div class="details_desc">{{{rich_description}}}</div>
            </div>
            <div class="col-md-4 hidden_phone">
                <img src="{{store_image}}" style="{{image_show}}" alt="Store Logo" class="details_image" />
                <div class="side_stores" style="{{show_box}}">
                    <a class="hover_underline" style="{{store_show}}" href="/stores/{{store_slug}}">{{store_name}}</a>
                    <br />
                    <a class="hover_underline" href="tel:{{phone}}" style="{{phone_show}}">{{phone}}</a>
                    <br style="{{phone_show}}" />
                    <a class="hover_underline" href="//{{website}}" target="_blank" style="{{show}}">Visit Website</a>
                </div>
            </div>
        </script>
    </div>
</div>
<script>
    $(document).ready(function(e){
        init(e)
        function renderAll(){
            var pathArray = window.location.pathname.split( '/' );
            var slug = pathArray[pathArray.length-1];
            job_details = getJobDetailsBySlug(slug);
            renderJobDetails('#promo_details_container', '#promo_details_template', job_details)
            show_content();
        }
        loadMallDataCached(renderAll); 
    })
</script>